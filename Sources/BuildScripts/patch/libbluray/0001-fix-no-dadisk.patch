diff --git forkSrcPrefix/src/file/mount_darwin.c forkDstPrefix/src/file/mount_darwin.c
index 5b13450e973fca6f44efee5111fa49ac71b65436..4cfdafcb99418efbc1a1c5bbe88c2c0ace7cf8e4 100644
--- forkSrcPrefix/src/file/mount_darwin.c
+++ forkDstPrefix/src/file/mount_darwin.c
@@ -30,12 +30,19 @@
 #define _DARWIN_C_SOURCE
 #include <sys/stat.h>
 
+#ifdef __APPLE__
+#include <TargetConditionals.h>
+#if TARGET_OS_OSX
 #include <DiskArbitration/DADisk.h>
+#endif
+#endif
 
 static char *bsdname_get_mountpoint(const char *device_path)
 {
     char *result = NULL;
 
+    #ifdef __APPLE__
+    #if TARGET_OS_OSX
     DASessionRef session = DASessionCreate(kCFAllocatorDefault);
     if (session) {
         DADiskRef disk = DADiskCreateFromBSDName(kCFAllocatorDefault, session, device_path);
@@ -57,6 +64,8 @@ static char *bsdname_get_mountpoint(const char *device_path)
         }
         CFRelease(session);
     }
+    #endif
+    #endif
 
     return result;
 }
diff --git forkSrcPrefix/meson.build forkDstPrefix/meson.build
index 08a47e05e194aeb56ff10fc2a42f631ef7c85e8f..833988b684a47efca74c26a067e385eed2500ce8 100644
--- forkSrcPrefix/meson.build
+++ forkDstPrefix/meson.build
@@ -52,7 +52,12 @@ endif
 
 if host_machine.system() == 'darwin'
     extra_dependencies += dependency('appleframeworks',
-        modules: ['CoreFoundation', 'DiskArbitration'])
+        modules: ['CoreFoundation'])
+    # Only add DiskArbitration framework if headers are available (macOS systems)
+    if cc.has_header('DiskArbitration/DiskArbitration.h')
+        extra_dependencies += dependency('appleframeworks',
+            modules: ['DiskArbitration'])
+    endif
 elif host_machine.system() == 'netbsd'
     test_args += '-D_NETBSD_SOURCE'
 elif host_machine.system() == 'openbsd'
